on:
  push:
    branches: [ "main" ]

env:
  IMAGE_NAME: jorlojor/invix-frontend:latest
  APP_DIR: invix-landing-page

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions: 
      contents: read
      packages: write
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: docker/setup-qemu-action@v3
      
      - uses: docker/setup-buildx-action@v3
      
      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: jorlojor
          password: dckr_pat_H7PWMTTR5xKFv2ngAh7X_Zwa_kA
      
      # ⭐ Tambah step untuk debug
      - name: Check files
        run: |
          cd ./invix-landing-page
          ls -la
          docker build -t  jorlojor/invix-frontend:latest.
          docker push jorlojor/invix-frontend:latest
      
      # - name: Build & Push
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: ${{ env.APP_DIR }}
      #     file: ${{ env.APP_DIR }}/Dockerfile
      #     # ⭐ Tambah platform
      #     platforms: linux/amd64
      #     push: true
      #     tags: ${{ env.IMAGE_NAME }}
      #     cache-from: type=registry,ref=${{ env.IMAGE_NAME }}
      #     cache-to: type=inline
      #     # ⭐ Tambah build args & progress output
      #     build-args: |
      #       NODE_OPTIONS=--max-old-space-size=6144
      #     # ⭐ Show full output
      #     outputs: type=docker
      #     provenance: false

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    
    steps:
      - name: SSH → deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            mkdir -p /srv/laresae/web
            
            cat >/srv/laresae/web/docker-compose.yml <<'YAML'
            version: "3.8"
            services:
              web:
                image: jorlojor/invix-frontend:latest
                container_name: laresae-frontend
                restart: unless-stopped
                ports:
                  - "80:80"
            YAML
            
            docker pull jorlojor/invix-frontend:latest
            docker compose -f /srv/laresae/web/docker-compose.yml up -d --remove-orphans
            docker image prune -f
